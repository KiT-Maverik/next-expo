/**
 * Creates a test ID based on the provided identifier.
 * For now this function returns identifier as is, but in the future,
 * environment-based conditional logic will be added.
 */
export const createTestId = (identifier: string) => identifier
